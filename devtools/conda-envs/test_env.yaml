name: test

channels:

  - conda-forge
  - defaults

dependencies:

  # from development
  - python
  - pip
  - numpy
  - tqdm
  - openmm
  - jupyterlab

  # here
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

